package com.varitas.gokulpos.tablet.utilities

object Enums {
    enum class ClickEvents {
        CANCEL, SAVE, VIEW, EDIT, PRICE, ERROR, QUANTITY, UPDATE, DELETE, LOGOUT, REPLACE, REDUCE, PRINT, LOAD, ITEMS, CLOSE, REMOVE, FAVOURITE, INVOICE
    }

    enum class Attributes {
        SIZE, PACK
    }

    enum class Menus {
        DEPARTMENT, CATEGORY, BRAND, SPECIFICATION, UOM, GROUP, ITEM
    }

    enum class Tender {
        CASH, CARD, FOODSTAMP
    }
}